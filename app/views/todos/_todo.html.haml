%li{:id => ['todo', todo.id]}
  %h3= "To Do for #{todo.created_at.strftime('%d %b %Y')}"
  %hr
  %ol.sortable.todo_tasks{'data-sort-url' => sort_tasks_path( :todo => todo.id ) }
    =render todo.tasks, :partial => 'tasks/task'
